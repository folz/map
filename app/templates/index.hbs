{{view Ember.Select content=locations value=selectedLocation optionLabelPath="content.name"}}

{{#if selectedLocation}}
    {{google-maps latitude=selectedLocation.latitude longitude=selectedLocation.longitude zoom="5"}}
{{/if}}
